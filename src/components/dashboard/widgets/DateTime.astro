<div class="flex flex-col items-center justify-center h-full space-y-2">
  <div id="date" class="text-lg font-medium text-gray-700 dark:text-gray-300"></div>
  <div id="clock" class="text-3xl font-bold text-gray-900 dark:text-gray-100"></div>
</div>

<script>
  function updateDateTime() {
    const now = new Date();
    
    // Update date in text format
    const dateOptions = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
    const dateText = now.toLocaleDateString('en-US', dateOptions);
    document.getElementById('date').textContent = dateText;
    
    // Update time in HH:mm format
    const timeText = now.toLocaleTimeString('en-US', { 
      hour: '2-digit', 
      minute: '2-digit',
      hour12: false 
    });
    document.getElementById('clock').textContent = timeText;
  }

  // Update immediately and then every second
  updateDateTime();
  setInterval(updateDateTime, 1000);
</script> 